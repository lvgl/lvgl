<!--
 @title Bar image indicator
 @brief Use bitmaps as the bar track and indicator — the bar clips each image to its part.

 `bg_image_src` replaces the solid background fill with pixels sampled from a
 bitmap. Set it on `selector="main"` for the background track and on
 `selector="indicator"` for the filled portion. As the value rises, the indicator
 image is revealed left-to-right, showing cyan segments that transition to yellow
 then red at the high end. Both images are 280×30, matching the bar widget size.
-->
<screen>
	<styles>
		<!-- Track: dark segmented bitmap as background -->
		<style name="style_bar_track" bg_image_src="img_bar_bg" bg_opa="0" radius="0" />
		<!-- Indicator: colour-coded segmented bitmap revealed left-to-right -->
		<style name="style_bar_indicator" bg_image_src="img_bar_indicator" bg_opa="0" radius="0" />
	</styles>

	<view flex_flow="column" style_flex_main_place="center" style_flex_cross_place="center" style_flex_track_place="center" style_pad_row="16">
		<!-- 💡 Drag the slider to change subject_value; the bar reveals more of the segmented indicator image as the value rises. -->
		<lv_bar name="bar" width="280" height="35" min_value="0" max_value="20" bind_value="subject_value2">
			<style name="style_bar_track" selector="main" />
			<style name="style_bar_indicator" selector="indicator" />
		</lv_bar>

		<lv_obj name="container"
			flex_flow="row"
            style_flex_cross_place="center"
            style_flex_main_place="space_between"
			style_pad_column="12"
			style_bg_opa="0"
			style_border_width="0"
			width="180"
			height="content"
		>
			<lv_button name="button_1">
				<lv_label name="label_1" align="center" text="-"  />
				<subject_increment_event subject="subject_value2" trigger="clicked" step="-1"/>
				<subject_increment_event subject="subject_value2" trigger="long_pressed_repeat"  step="-1"/>
			</lv_button>
			<lv_label name="label_2" bind_text="subject_value2" />
			<lv_button name="button_2">
				<lv_label name="label_3" align="center" text="+"  />
				<subject_increment_event subject="subject_value2" trigger="clicked"  step="1"/>
				<subject_increment_event subject="subject_value2" trigger="long_pressed_repeat" step="1" />
			</lv_button>
		</lv_obj>
	</view>
</screen>