<!--
 @title Dropdown bind value
 @brief Dropdown bound to a subject; a button jumps it programmatically.

 The dropdown's `bind_value` is wired to `subject_index`. A "Pick Cherry" button uses
 `subject_set_int_event` to write `2` into the subject on click — the dropdown
 picks up the change and updates the selection without any direct call into
 the dropdown widget.
-->
<screen>
	<view flex_flow="column" style_flex_main_place="center" style_flex_cross_place="center" style_flex_track_place="center" style_pad_row="16">
		<!-- 💡 Open the dropdown to pick an option, or click the button to snap to a fixed one through the subject. -->
		<lv_label name="label_1" bind_text="subject_index" bind_text-fmt="Selected option: %d"/>

		<lv_dropdown name="dropdown" width="160" options="Apple&#10;Banana&#10;Cherry&#10;Date&#10;Elderberry" bind_value="subject_index" />

		<lv_button name="button">
			<lv_label name="label_2" align="center" text="Pick Cherry" />
			<subject_set_int_event subject="subject_index" value="2" />
		</lv_button>
	</view>
</screen>
