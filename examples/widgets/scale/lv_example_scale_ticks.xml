<!--
 @title Scale tick configuration
 @brief Tune tick density and which ticks count as "major" (drawn longer with labels).

 `total_tick_count` is the *total* number of ticks rendered — both minor and
 major. `major_tick_every` picks every Nth tick to be drawn longer and
 (when `label_show="true"`) carry a numeric label. The three scales below
 share the same physical width but vary their tick density.
-->
<screen>
	<styles>
		<style name="style_major" length="8" line_width="3" />
	</styles>
	<view flex_flow="column" style_flex_main_place="space_evenly" style_flex_track_place="center" style_pad_row="20">
		<!-- 💡 Bump `total_tick_count` or change `major_tick_every` to see how density and labels reorganise. -->
		<!-- Sparse: 5 ticks total, every second one major -->
		<lv_scale
			name="scale_1"
			width="260"
			height="30"
			mode="horizontal_bottom"
			total_tick_count="5"
			major_tick_every="2"
			label_show="true"
			min_value="0"
			max_value="100"
		>
			<style name="style_major" selector="indicator" />
		</lv_scale>
		<!-- Medium: 11 ticks, every 5th major -->
		<lv_scale
			name="scale_2"
			width="260"
			height="30"
			mode="horizontal_bottom"
			total_tick_count="11"
			major_tick_every="5"
			label_show="true"
			min_value="0"
			max_value="100"
		>
			<style name="style_major" selector="indicator" />
		</lv_scale>

		<!-- Dense: 21 ticks, every 4th major — minor ticks become visible -->
		<lv_scale
			name="scale_3"
			width="260"
			height="30"
			mode="horizontal_bottom"
			total_tick_count="21"
			major_tick_every="4"
			label_show="true"
			min_value="0"
			max_value="100"
		>
			<style name="style_major" selector="indicator" />
		</lv_scale>
	</view>
</screen>
