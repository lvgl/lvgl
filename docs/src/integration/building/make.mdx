---
title: make
description: LVGL can be easily integrated into any Makefile project by adding include lvgl/lvgl.mk to the main Makefile.
---

LVGL can be easily integrated into any Makefile project by
adding `include lvgl/lvgl.mk` to the main `Makefile`.
