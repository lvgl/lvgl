---
title: OpenGL Overview
description: "How LVGL's OpenGL support fits together on Linux: which display drivers create a context, which renderers render on the GPU, and how the two combine."
---

## Overview

OpenGL is a cross-platform, hardware-accelerated graphics API, and OpenGL ES is the subset designed for
embedded and mobile hardware. LVGL uses it on Linux for three reasons: to offload rendering to the GPU, to
embed LVGL content inside existing OpenGL applications, and to render 3D content.

LVGL's OpenGL support comes in two halves, and you generally need one of each:

- **Display drivers** create and own the OpenGL context, and put the finished frame on screen.
- **Renderers** do the actual rendering with OpenGL instead of the CPU.

Enabling a driver with an OpenGL backend but no GPU renderer is valid and sometimes what you want,
LVGL can then render using software and use OpenGL to present the result to the display.

## Drivers That Create a Context

These drivers create and manage an OpenGL context for you:

| Driver | How to get the OpenGL path |
|---|---|
| [DRM](/main-modules/display_drivers/drm) | `LV_LINUX_DRM_BACKEND_EGL` |
| [Wayland](/main-modules/display_drivers/wayland) | `LV_WAYLAND_USE_EGL` |
| [SDL](/main-modules/display_drivers/sdl) | `LV_SDL_BACKEND_EGL` |
| [GLFW](/main-modules/display_drivers/glfw) | Always |

Every one of them except GLFW uses [EGL](/main-modules/display_drivers/egl) underneath.

<Callout type="warning" title="GLFW and EGL are mutually exclusive">
<ApiLink name="LV_USE_GLFW" /> cannot be combined with <ApiLink name="LV_USE_EGL" /> 
</Callout>

Additionally there's also a generic [OpenGL texture driver](/main-modules/display_drivers/opengl_texture)
which can be use to embed LVGL in an application that already owns the context.

## Renderers

| Renderer | Notes |
|---|---|
| [NanoVG](/main-modules/renderers/nanovg) | **Recommended.** Rasterizes on the GPU, best performance and widest feature coverage |
| [OpenGL ES](/main-modules/renderers/opengles) | Caches software-rendered areas as GPU textures. Slower, fewer features. |

## 3D and glTF

The glTF module loads and renders 3D models per the glTF 2.0 specification, with PBR materials, animations and
camera control. It needs OpenGL ES 2.0 with some extra extensions.

3D support comes from the GL renderers so a GPU renderer is required.

For details, see [glTF](/libs/gltf).

## See Also

- [NanoVG](/main-modules/renderers/nanovg) - the recommended GPU renderer
- [EGL](/main-modules/display_drivers/egl) - the layer under most OpenGL paths on Linux
- [DRM](/main-modules/display_drivers/drm) - production driver with an EGL backend
- [GLFW](/main-modules/display_drivers/glfw) - quickest OpenGL setup for development
- [OpenGL texture](/main-modules/display_drivers/opengl_texture) - embedding LVGL in an existing context
- [glTF](/libs/gltf) - 3D model rendering
