/**
 * @file target.h
 * Board constants for QEMU's virt machine. This target reads an exact instruction counter,
 * so there is no SysTick scale factor to set; the file exists because plat.h includes one
 * for every target.
 */
#ifndef TARGET_H
#define TARGET_H
#endif /*TARGET_H*/
