# Kconfig file for LVGL v9.5.0
#
menu "LVGL"

	config LVGL_VERSION_MAJOR
		int
		default 9
	config LVGL_VERSION_MINOR
		int
		default 6
	config LVGL_VERSION_PATCH
		int
		default 0

	menu "Build"
		rsource "env_support/kconfig/Kconfig.build"
	endmenu 

	menu "Memory and Standard Library"
		rsource "src/stdlib/Kconfig"
	endmenu 

	menu "Rendering Configuration"
		rsource "src/draw/Kconfig"
	endmenu

	menu "Input Devices"
		rsource "src/indev/Kconfig"
	endmenu

	menu "Core"
		rsource "src/core/Kconfig"
	endmenu

	menu "Operating System (OS)"
		rsource "src/osal/Kconfig"
	endmenu

	menu "Logging"
		rsource "src/logging/Kconfig"
	endmenu

	menu "Debugging"
		rsource "src/debugging/Kconfig"
	endmenu


	menu "Feature Configuration"
		menu "Others"
		endmenu
	endmenu

	menu "Compiler Settings"
		config LV_BIG_ENDIAN_SYSTEM
			bool "For big endian systems set to 1"
			help
				For big endian systems set to 1

		config LV_ATTRIBUTE_MEM_ALIGN
			string "Alignment attribute"
			default ""
			help
		        	Added where memory needs to be aligned. With -Os, data might not be
				aligned to the required boundary by default.
				E.g. __attribute__((aligned(4)))

		config LV_ATTRIBUTE_FAST_MEM_USE_IRAM
			bool "Set IRAM as LV_ATTRIBUTE_FAST_MEM"
			help
				Set this option to configure IRAM as LV_ATTRIBUTE_FAST_MEM
	endmenu

	
	menu "Enable static fonts"
	endmenu

	menu "Image Settings"
		rsource "src/image/Kconfig"
	endmenu

	menu "Text & Font Settings"
		rsource "src/font/Kconfig"
	endmenu

	menu "Widgets"
		rsource "src/widgets/Kconfig"
	endmenu

	menu "Themes"
		rsource "src/themes/Kconfig"
	endmenu

	menu "Layouts"
		rsource "src/layouts/Kconfig"
	endmenu

	menu "File System"
		rsource "src/fs/Kconfig"
	endmenu

	menu "Drivers"
		rsource "src/drivers/Kconfig"
	endmenu

	menu "Examples"
		rsource "examples/Kconfig"
	endmenu

	menu "Demos"
		rsource "demos/Kconfig"
	endmenu

endmenu
